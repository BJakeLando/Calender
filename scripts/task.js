class Task {
  constructor(title, desc, dueDate, tag, color, cat) {
    this.title = title;
    this.description = desc;
    this.dueDate = dueDate;
    this.tag = tag;
    this.color = color;
    this.category = cat;

    this.name = "Brandon";
  }
}

function init() {}

window.onload = init;
